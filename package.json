{
  "name": "scaleofus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scale of Us - small books about big machines and human scale.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "build:pages": "astro build && mkdir -p docs && cp -R dist/. docs/",
    "render:sketches": "node scripts/render-remarkable-sketches.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/newsreader": "^5.2.10",
    "@fontsource-variable/playwrite-ie": "^5.2.7",
    "astro": "^6.2.1"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
